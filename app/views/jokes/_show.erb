<section>
  <% if @joke %>
    <p><%= "#{@joke.content}" %></p>
    <% if current_user && @display_button%>
      <a href='/favorite/<%= @joke.id %>' name='favorite' value='<%= @joke.id %>' %>Favorite this joke.</a>
    <% end %>
  <% end %>
</section>
